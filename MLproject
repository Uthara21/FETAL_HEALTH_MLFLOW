name: tutorial

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      n_estimators: int
      max_depth:    int
    command: "python fetal_health.py {n_estimators} {max_depth}"
